/**
 * Menu item categories. Mirrors the frontend `FoodType` enum
 * (pingo-pro-game-angular/src/app/modules/menu/enum/food-type.enum.ts) — keep aligned.
 */
export enum FoodType {
  SNACKS     = 'Snacks',
  FOOD       = 'Food',
  DRINK      = 'Drink',
  HOT_FOOD   = 'HotFood',
  COLD_FOOD  = 'ColdFood',
  HOT_DRINK  = 'HotDrink',
  COLD_DRINK = 'ColdDrink',
}
