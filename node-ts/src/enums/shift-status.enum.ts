/**
 * Open/closed state of an employee Shift.
 * Values are the persisted strings — do not change them without a data migration.
 */
export enum ShiftStatus {
  Open = 'open',
  Closed = 'closed',
}
