/**
 * Device/facility types. Mirrors the frontend `DeviceType` enum
 * (pingo-pro-game-angular/src/app/core/enums/device-type.enum.ts) — keep aligned.
 */
export enum DeviceType {
  PC        = 'PC',
  ROOM      = 'ROOM',
  XBOX      = 'XBOX',
  PINGPONG  = 'PINGPONG',
  BILLIARDS = 'BILLIARDS',
}
